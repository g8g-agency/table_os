require('dotenv').config();
const jwt = require('jsonwebtoken');

// Let's decode the access_token from the app if we know it.
// We don't have the token.
// Let's create a test to directly call the exchange endpoint.
